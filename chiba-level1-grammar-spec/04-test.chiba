namespace lexer.spec.strings.basic

def strings(): i32 = {
    let plain = "hello world"
    let escaped = "line1\nline2\tindent"
    let c_text = c"hello from c abi"
    let raw_one = r"raw keeps \\n text"
    let raw_hash = r#"raw can contain "quotes""#
    let trick = ###"##"##" 
    ;;"###;;
    let prefixed = sql"select * from users where id = 1"
    let prefixed_raw = sqlr#"select "name" from users"#
    return 0
}
