namespace lexer.spec.strings.multiline

def multiline(name: String, id: i64): String = {
    let normal = "hello
world
${name}"
    let delimited = ##"
first line
${id}
"##
    let raw_multi = r##"
${name} stays text in raw mode
"##
    return normal
}
