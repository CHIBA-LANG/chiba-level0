namespace lexer.spec.statement.boundaries

def statement_boundaries(): i32 = {
    let a = 1
    let b = 2; let c = 3
    call(a) {|x|
        x + 1
    }

    let same_line_is_not_separator = 1 let next_token_same_line = 2
    return a + b + c
}
