namespace lexer.spec.layout.continuation

def continuation_layout(value: i64): i64 = {
    let x = value +
        1 *
        2
    let y = value |>
        step1() |>
        step2(_)
    let z = if
        value > 0 {
        value
    } else {
        0
    }
    return x + y + z
}
