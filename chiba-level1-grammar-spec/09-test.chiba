namespace lexer.spec.delimiters

def delimiter_newlines(): i32 = {
    let grouped = (
        1 +
        2
    )
    let tuple = (
        grouped,
        grouped + 1,
    )
    let slice = [
        grouped,
        grouped + 1,
    ]
    let indexed = slice[
        0
    ]
    return indexed
}
