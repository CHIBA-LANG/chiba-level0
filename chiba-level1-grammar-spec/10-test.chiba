namespace lexer.spec.trailing.closure

def trailing_closure(xs: Vec): Vec = {
    let mapped = map(xs) {|x|
        x + 1
    }
    let empty = with_scope() {||
        0
    }
    let typed = fold(xs, 0) {|acc: i64, x: i64|
        acc + x
    }
    return mapped
}
