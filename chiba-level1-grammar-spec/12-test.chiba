namespace lexer.spec.tuples.slices

def tuple_slice_index(text: String): i64 = {
    let unit = ()
    let grouped = (1)
    let one_tuple = (1,)
    let pair = (1, 2)
    let nested = ((1,), (2, 3))
    let bytes = text[0..4]
    let cp = text.codepoint_at(0)
    return cp
}
