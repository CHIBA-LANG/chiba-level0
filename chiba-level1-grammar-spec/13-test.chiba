namespace lexer.spec.patterns

def patterns(value: Option[Point]): i64 = {
    match value {
        whole @ Some({ x, y: inner @ Some(z) }) if z > 0 => z
        Some((a, _)) => a
        None => 0
        :ok => 1
        0 => 2
        true => 3
        _ => 4
    }
}
