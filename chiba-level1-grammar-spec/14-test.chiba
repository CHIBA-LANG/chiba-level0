namespace lexer.spec.letdestruct

def destruct(point: Shape): i64 = {
    let (x, 
    (y, z)) = point.coords
    let { name, meta: { age, active } } = point
    let whole @ { center: { x: cx, y: 
    cy }, color } = point
    let _ = active
    return x + y + z + age + cx + cy
}
