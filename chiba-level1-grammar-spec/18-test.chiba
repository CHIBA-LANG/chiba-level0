namespace lexer.spec.lambda

def closures(x: i64): i64 = {
    let inc = (v: i64): i64 => v + 1
    let blocky = (): i64 => {
        let local = x + 1
        return local
    }
    let pair = (a: i64, b: i64): i64 => {
        return a + b
    }
    return inc(blocky()) + pair(1, 2)
}
