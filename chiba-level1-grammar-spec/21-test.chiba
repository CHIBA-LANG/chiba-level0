namespace lexer.spec.paths.generics

use demo.
math.
add
use demo.io.{print, println}
use demo.
prelude.*

def generic_paths(xs: Vec[Option[Result[i64, :error]]]): Option[i64] = {
    let value = Option.Some(Result.Ok(1))
    return value
}
