#![CBI]

namespace lexer.spec.attribute.targets

#[entry]
def main(): i32 = {
    #[trace]
    let value = 1

    #[inline]
    unsafe {
        use_low_level()
    }
    call(value) #[tail]
    {|x|
        x + 1
    }
    return 0
}
