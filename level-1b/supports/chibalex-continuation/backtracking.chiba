namespace level1b.chibalex_continuation.backtracking

type Candidate {
    end: usize,
}

def choose_candidate(candidate: Candidate): Candidate = reset {
    shift retry {
        let first = retry(candidate)
        retry(first)
    }
}
